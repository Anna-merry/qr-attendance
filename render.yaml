services:
  - type: web
    name: qr-attendance
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT app:app"
    envVars:
      - key: PYTHON_VERSION
        value: "3.12.9"   # ← ЯВНО указываем версию